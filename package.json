{
  "name": "hydractrl",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist",
    "build:exe": "bun build src/index.ts --compile --outfile hydractrl",
    "lint": "biome check src/",
    "format": "biome format --write src/",
    "test": "bun test"
  },
  "dependencies": {
    "hydra-synth": "^1.3.25"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "bun-types": "latest"
  }
}
